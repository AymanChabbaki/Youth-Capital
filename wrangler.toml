# CHANGE "name" to match the exact project name shown in your Cloudflare
# dashboard (Workers & Pages -> your project). If it doesn't match, Wrangler
# will try to deploy to a different/new project instead of this one.
name = "youth-capital"
compatibility_date = "2026-08-24"

[assets]
directory = "./frontend/dist"
not_found_handling = "single-page-application"
